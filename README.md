1. (Relative Stability) -> Anchored or Pegged => USD
2. (Stability Mechanism) -> Minting  => Algorithmnic (Decentralised)
3. (Collateral) -> Exogenous (Crypto)


The below are some commands that you can try to run the code 

try running hardhat node in a different terminal 

``` shell

yarn hardhat compile 

yarn hardhat deploy 

yarn hardhat node 

yarn hardhat deploy --network localhost 

yarn hardhat test --network localhost

```
